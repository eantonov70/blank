import type { AuthClientTypes } from '@walletconnect/auth-client';
import type { WalletConnectModalConfig } from '@walletconnect/modal';
export interface WalletConnectModalAuthOptions {
    projectId: string;
    metadata: AuthClientTypes.Metadata;
    modalOptions?: Omit<WalletConnectModalConfig, 'chains' | 'enableAuthMode' | 'projectId' | 'walletConnectVersion'>;
}
export interface WalletConnectModalAuthSignInArguments {
    statement: string;
    chainId?: string;
    aud?: string;
    domain?: string;
}
export declare class WalletConnectModalAuth {
    #private;
    constructor(options: WalletConnectModalAuthOptions);
    signIn(args: WalletConnectModalAuthSignInArguments): Promise<{
        valid: boolean;
        address: string;
        cacao: Record<string, string>;
    }>;
}
